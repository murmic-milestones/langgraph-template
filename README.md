# langgraph-template
A minimal, modern **LangGraph 1.x** hello-world project. It implements a tiny two-stage chatbot — collect the user's name, then chat — purely as a vehicle for the architecture patterns below. Replace the agents with your own and keep the skeleton.
